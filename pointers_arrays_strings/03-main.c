#include "main.h"

/**
 * main- check the code
 *
 * Return: always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. Ifear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
